/**
 * @param {number[]} pref
 * @return {number[]}
 */
var findArray = function(pref) {
    let n = pref.length;
    let result = Array(n).fill(0);
    result[0] = pref[0];
    for(let i = 1; i < n; i++) {
        result[i] = pref[i - 1] ^ pref[i];
    }
    return result;
};