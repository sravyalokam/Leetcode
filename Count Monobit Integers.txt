/**
 * @param {number} n
 * @return {number}
 */
var countMonobit = function(n) {
    let count = 1; 
    let num = 1;   

    while (num <= n) {
        count++;
        num = (num << 1) | 1; 
    }

    return count;
};